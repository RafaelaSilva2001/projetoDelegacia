<!DOCTYPE html>
<html lang="pt-br">
<html data-bs-theme="dark"></html>
<head>
    <meta charset="UTF-8" />
    <title>Editar Policial</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
    <div class="container mt-4">
        <h1 class="mb-4">Editar Policial</h1>
        <form action="/policiais/atualizar/<%= policial._id %>" method="POST" class="needs-validation" novalidate>
            <div class="mb-3">
                <label for="matricula" class="form-label">Matrícula:</label>
                <input type="number" id="matricula" name="matricula" value="<%= policial.matricula %>" class="form-control" required />
                <div class="invalid-feedback">
                    Por favor, insira a matrícula.
                </div>
            </div>

            <div class="mb-3">
                <label for="nome" class="form-label">Nome:</label>
                <input type="text" id="nome" name="nome" value="<%= policial.nome %>" class="form-control" required />
                <div class="invalid-feedback">
                    Por favor, insira o nome.
                </div>
            </div>

            <div class="mb-3">
                <label for="telefone" class="form-label">Telefone:</label>
                <input type="text" id="telefone" name="telefone" value="<%= policial.telefone %>" class="form-control" />
            </div>

            <div class="mb-3">
                <label for="patente" class="form-label">Patente:</label>
                <input type="text" id="patente" name="patente" value="<%= policial.patente %>" class="form-control" />
            </div>

            <button type="submit" class="btn btn-primary">Salvar Alterações</button>
            <a href="/policiais/relatorio" class="btn btn-secondary ms-2">Cancelar</a>
        </form>
    </div>

    <script>
        // Bootstrap form validation
        (function () {
            'use strict'
            var forms = document.querySelectorAll('.needs-validation')
            Array.prototype.slice.call(forms)
                .forEach(function (form) {
                    form.addEventListener('submit', function (event) {
                        if (!form.checkValidity()) {
                            event.preventDefault()
                            event.stopPropagation()
                        }
                        form.classList.add('was-validated')
                    }, false)
                })
        })()
    </script>
</body>
</html>
